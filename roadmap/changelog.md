# プロジェクト変更履歴

このプロジェクトへのすべての重要な変更はこのファイルに記録されます。

## [未リリース]

## [2026-01-31]
### 追加
- プロジェクトの履歴と変更（ユーザーおよびAntigravityによる）を追跡するための `changelog.md` を作成しました。
- 既存のプロジェクト構造とステータス（`tasks.json`, `implementation_plan.json`）を確認しました。

### 変更
- 変更履歴のタスクを含めるため `task.md` を更新しました。
- `Header.astro` のテーマ切り替え時のアニメーションタイミングを修正しました。
    - ロゴ画像の切り替えを `display: none` から `opacity` によるクロスフェードに変更。
    - ロゴテキストとメニューボタンに `color` トランジションを追加。
    - アイコンとスイッチのつまみにトランジションを適用し、全体のタイミング（0.3s）を統一。
