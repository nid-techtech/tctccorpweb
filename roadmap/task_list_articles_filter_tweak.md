# タスクリスト: フィルターパネル・タグ表記の微調整

1. モバイル用のフィルター展開ボタンのテキストを「フィルター」から「絞り込んで検索」へ変更。
2. `.filter-pill span` コントロールの余白(`padding`)を極小にし、文字サイズ(`font-size`)を縮小する。
3. 記事カード内の要素タグ (`.tag-link`) に対して、上記で調整した `.filter-pill span` と同等のスタイルとホバー時の色反転アニメーション（背景色と文字色の反転など）を適用・統一する。
