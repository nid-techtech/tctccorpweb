# Walkthrough

1. `Header.astro` ファイルを編集し、デスクトップメニューおよびモバイルメニューの両方で、「WORKS」のリンク先を `/articles?category=works` に書き換え、かつ「ALL」と表示されていたテキストを「ARTICLES」に更新しました。
2. ディレクトリ `src/pages/[tag]` に対して `rm -rf '[tag]'` コマンドを実行し、不要になったタグ検索用ページ（slug）を削除しました。
3. すべての変更が完了したため、タスクリスト、実装計画、およびこのウォークスルーのドキュメントを `roadmap/` の下に作成・配置しました。
